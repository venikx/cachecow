{
  "name": "@cachecow/image-optimization",
  "version": "0.1.0",
  "scripts": {
    "prebuild": "npm install sharp --cpu=x64 --os=linux --libc=glibc --prefix functions/image-optimization",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.150",
    "@types/node": "^24.0.10",
    "tsx": "^4.20.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.842.0",
    "sharp": "^0.34.2"
  }
}
